

🤖 **Telegram Utility Bot**
Python • Telebot • REST API • Random

## 📖 О проекте
Универсальный Telegram-бот с набором развлекательных и полезных функций. Бот разделен на тематические блоки команд, что делает взаимодействие с ним интуитивно понятным. Проект демонстрирует работу с внешними API, обработку пользовательского ввода и организацию команд по категориям.

## ✨ Основные возможности
- **🎲 Рандомизация** - генерация паролей, эмодзи, подброс монетки
- **🐾 Животные** - получение случайных изображений уток, лис, собак и кошек через внешние API
- **😂 Развлечения** - отправка мемов и смешков
- **📁 Локальные ресурсы** - работа с локальной коллекцией изображений-мемов
- **🏷️ Тематические блоки** - команды сгруппированы по категориям для удобства
- **⚙️ Утилиты** - полезные инструменты в одном месте

## 🏗️ Архитектура проекта
### Структура файлов:
```
├── main.py           # Основной файл бота (обработчики команд Telegram)
├── bot_logic.py      # Бизнес-логика (генераторы, API-запросы)
├── images/           # Папка с локальными мемами
│   ├── meme1.jpg
│   ├── meme2.png
│   └── ...
└── config.py         # Конфигурация (токен бота) *рекомендуется добавить*
```

### Блоки команд:
- **🦮 `animals`** - изображения животных (утки, лисы, собаки, кошки)
- **😄 `fun`** - развлекательные функции (мемы, смешки)
- **🎮 `games`** - простые игры (подброс монетки)
- **🛠️ `utils`** - утилиты (генератор паролей, эмодзи)

## 🚀 Установка и запуск
### Требования:
- Python 3.8+
- Аккаунт в Telegram
- Токен бота от [@BotFather](https://t.me/BotFather)

### Шаги установки:
1. **Клонируйте и перейдите в директорию:**
```bash
git clone https://github.com/your-username/tg_bot.git
cd tg_bot
```

2. **Установите зависимости:**
```bash
pip install pytelegrambotapi requests
```

3. **Настройте конфигурацию:**
   - Создайте файл `config.py`
   - Добавьте ваш токен:
   ```python
   TOKEN = "ВАШ_ТОКЕН_ЗДЕСЬ"
   ```
   - В `main.py` замените строку на: `bot = telebot.TeleBot(TOKEN)`

4. **Подготовьте папку с мемами:**
```bash
mkdir images
# Добавьте ваши мемы в формате .jpg/.png в папку images/
```

5. **Запустите бота:**
```bash
python main.py
```

## 📱 Использование бота
### Доступные команды:
| Команда | Описание | Пример |
|---------|----------|---------|
| `/start` | Начало работы с ботом | `/start` |
| `/hello` | Приветствие | `/hello` |
| `/bye` | Прощание | `/bye` |
| `/pass` | Генератор пароля | `/pass` → ввод длины |
| `/emodji` | Случайный эмодзи | `/emodji` |
| `/coin` | Подброс монетки | `/coin` |
| `/heh` | Смех заданное число раз | `/heh 7` |
| `/mem` | Случайный мем | `/mem` |
| `/duck` | Изображение утки | `/duck` |
| `/fox` | Изображение лисы | `/fox` |
| `/dog` | Изображение собаки | `/dog` |
| `/cat` | Изображение кошки | `/cat` |
| **🎮 Блочные команды** | |
| `/games` | Случайная игра | `/games` |
| `/animals` | Случайное животное | `/animals` |
| `/fun` | Случайное развлечение | `/fun` |
| `/utils` | Случайная утилита | `/utils` |

## 🎮 Примеры использования
### Генерация пароля:
```
👤 Пользователь: /pass
🤖 Бот: Укажите длину пароля (число от 1 до 15):
👤 Пользователь: 10
🤖 Бот: Вот твой сгенерированный пароль длиной 10 символов: &$#@12<>?!
```

### Случайное животное:
```
👤 Пользователь: /animals
🤖 Бот: *случайно отправляет изображение лисы, утки, собаки или кошки*
```

### Тематический блок развлечений:
```
👤 Пользователь: /fun
🤖 Бот: *случайно отправляет мем или серию "hehehe..."*
```

## 🔧 Технические детали
### Используемые технологии:
- **Telebot** - Python-библиотека для Telegram Bot API
- **Requests** - выполнение HTTP-запросов к внешним API
- **Random** - генерация случайных данных
- **OS** - работа с файловой системой

### Внешние API:
| Сервис | Функция | Endpoint |
|--------|---------|----------|
| Random Duck API | Изображения уток | `https://random-d.uk/api/random` |
| Random Dog API | Изображения собак | `https://random.dog/woof.json` |
| Random Fox API | Изображения лис | `https://randomfox.ca/floof/` |
| The Cat API | Изображения кошек | `https://api.thecatapi.com/v1/images/search` |

### Ключевые функции из `bot_logic.py`:
```python
def gen_pass(pass_length):     # Генерация пароля из спецсимволов и цифр
def gen_emodji():              # Возврат случайного эмодзи из списка
def flip_coin():               # Симуляция подбрасывания монетки
```

## 📁 Структура команд бота
```
┌── Стандартные
│   ├── /start, /hello, /bye
│   └── Эхо-ответ на любой текст
│
├── 🐾 Animals Block
│   ├── /duck, /fox, /dog, /cat
│   └── /animals (рандом из всех)
│
├── 😄 Fun Block
│   ├── /heh, /mem
│   └── /fun (рандом из всех)
│
├── 🎮 Games Block
│   └── /coin, /games
│
└── 🛠️ Utils Block
    ├── /pass, /emodji
    └── /utils (рандом из всех)
```

## 🚀 Возможные улучшения
1. **База данных** - добавление SQLite для хранения пользовательских данных
2. **Конфигурационный файл** - вынос токена и настроек в отдельный файл
3. **Логирование** - добавление системы логов для отладки
4. **Больше API** - интеграция с погодой, курсами валют, новостями
5. **Inline-режим** - возможность использовать бота в любом чате
6. **Клавиатуры** - добавление Reply и Inline клавиатур для удобства

---

**Сделано с ❤️ для любителей Telegram и полезных ботов!**

---

*Примечание: Для реального использования не забудьте вынести токен бота в отдельный конфигурационный файл или переменные окружения.*
